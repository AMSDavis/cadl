import "@cadl-lang/rest";

using Cadl.Rest;
using Cadl.Rest.Resource;

model Credential {
  @doc("Data source credential")
  @format("UUID")
  @visibility("read")
  @segment("credentials")
  @key
  credentialId: string;
}
