<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <BuildId Condition="'$(BuildId)' == ''">0</BuildId>
    <TargetFramework>netstandard2.0</TargetFramework>
    <PackageId>Microsoft.Cadl.Providerhub.Controller</PackageId>
      <Version>1.0.0-beta.$(BuildId)</Version>
      <PackageTags>Microsoft Azure Cadl RPaas</PackageTags>
      <Description>
        This common class library for RPaas controller extensions
        For this release see notes - TBA
        in addition to the breaking changes TBA
        Cadl quickstarts and tutorials - https://github.com/Azure/adl
      </Description>
      <WarningLevel>9999</WarningLevel>
      <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Core" Version="2.2.5" />
  </ItemGroup>

  <!-- TODO review and move into common props and targets when multiple csproj exists.-->
  <PropertyGroup>
    <SignAssembly>true</SignAssembly>
    <AssemblyOriginatorKeyFile>../../eng/MSSharedLibKey.snk</AssemblyOriginatorKeyFile>
    <DelaySign>true</DelaySign>
  </PropertyGroup>
</Project>
